naftiko: 1.0.0-alpha2
info:
  label: Foursquare Places API — Tips
  description: 'Tips surface of the Foursquare Places API. 1 operation(s). Lead operation: Get Place Tips.
    Self-contained Naftiko capability covering one Foursquare business surface.'
  tags:
  - Foursquare
  - Places
  - Tips
  created: '2026-05-04'
  modified: '2026-06-02'
binds:
- namespace: env
  keys:
    FOURSQUARE_SERVICE_TOKEN: FOURSQUARE_SERVICE_TOKEN
capability:
  consumes:
  - type: http
    namespace: places-tips
    baseUri: https://places-api.foursquare.com
    description: Foursquare Places API — Tips business capability. Self-contained, no shared references.
    authentication:
      type: bearer
      token: '{{env.FOURSQUARE_SERVICE_TOKEN}}'
    resources:
    - name: places-fsq-place-id-tips
      path: /places/{fsq_place_id}/tips
      operations:
      - name: getPlaceTips
        method: GET
        description: Get Place Tips
        inputParameters:
        - name: X-Places-Api-Version
          in: header
          type: string
          required: true
          description: Dated Places API version header.
        - name: fsq_place_id
          in: path
          type: string
          required: true
          description: fsq_place_id
        - name: limit
          in: query
          type: integer
          required: false
          description: limit
        - name: sort
          in: query
          type: string
          required: false
          description: sort
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: places-tips-rest
    port: 8080
    description: REST adapter for Foursquare Places API — Tips. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/places-fsq-place-id-tips
      name: places-fsq-place-id-tips
      description: REST surface for Get Place Tips.
      operations:
      - method: GET
        name: getPlaceTips
        description: Get Place Tips
        call: places-tips.getPlaceTips
        with:
          X-Places-Api-Version: rest.X-Places-Api-Version
          fsq_place_id: rest.fsq_place_id
          limit: rest.limit
          sort: rest.sort
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: places-tips-mcp
    port: 9090
    transport: http
    description: MCP adapter for Foursquare Places API — Tips. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: get-place-tips
      description: Get Place Tips
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: places-tips.getPlaceTips
      with:
        X-Places-Api-Version: tools.X-Places-Api-Version
        fsq_place_id: tools.fsq_place_id
        limit: tools.limit
        sort: tools.sort
      outputParameters:
      - type: object
        mapping: $.
